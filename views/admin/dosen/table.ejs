<table id="example2" class="table table-bordered table-hover">
    <thead>
    <tr>
      <th>No</th>
      <th>Nama</th>
      <th>Email</th>
      <th>No Wa</th>
      <th>Pendidikan</th>
      <th>Jumlah Bimbingan</th>
      <th>Status</th>
      <th>Aksi</th>
    </tr>
    </thead>
    <tbody>
      <% for(let i = 0; i < dosen.length; i++){ %>
        <tr>
          <td><%= i + 1 %></td>
          <td><%= dosen[i].nama %>, <%= dosen[i].gelar %> </td>
          <td><%= dosen[i].email %> </td>
          <td><%= dosen[i].no_wa %> </td>
          <td>
            <% if(dosen[i].pendidikan == 0.5) { %>
              S2
            <% } else if(dosen[i].pendidikan == 1) { %>
              S3
            <% } %>
          </td>
          <td>
            <% if(dosen[i].jumlah == 0) { %>
              Sangat Banyak
            <% } else if(dosen[i].jumlah == 0.25) { %>
              Banyak
            <% } else if(dosen[i].jumlah == 0.5) { %>
              Cukup
            <% } else if(dosen[i].jumlah == 0.75) { %>
              Sangat Sedikit
            <% } else if(dosen[i].jumlah == 1) { %>
              Sedikit
            <% } %>
          </td>
          <td>
            <% if(dosen[i].status === 'Y') { %>
              <span class="badge badge-success">
                Aktif
              </span>
            <% } else { %>
              <span class="badge badge-danger">
                Tidak aktif
              </span>
            <% } %>
          </td>
          <td>
            <form action="/dosen/delete/<%= dosen[i]._id %>?_method=DELETE" method="post">
              <a href="/dosen/status/<%= dosen[i]._id %>?_method=PUT" class="btn btn-primary btn-sm">
                <% if(dosen[i].status === 'Y') { %>
                  <i class="fas fa-user-slash"></i>
                    Matikan
                <% } else { %>
                  <i class="fas fa-user-check"></i>
                    Hidupkan
                <% } %>
              </a>

              <a href="/dosen/edit/<%= dosen[i]._id %>" class="btn btn-info btn-sm"  title="Ubah">
                <i class="fas fa-pencil-alt"></i>
                Ubah
              </a>

              <button class="btn btn-danger btn-sm" type="submit" title="Hapus">
                <i class="fas fa-trash"></i>
                Hapus
              </button>

            </form>
          </td>
        </tr>
      <% } %>

    </tbody>
    </tfoot>
  </table>